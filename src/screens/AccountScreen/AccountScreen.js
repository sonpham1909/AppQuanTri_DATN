import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const AccountScreen = ({navigation}) => {
  return (
    <View>
      <Text>Đây là màn hình quản lý tài khoản</Text>
    </View>
  )
}

export default AccountScreen

const styles = StyleSheet.create({})